# Frontend

React/Vite chat client for the Agentic RAG backend. See the repository [README](../README.md) for setup and run instructions.

The API origin is configured with `VITE_API_URL` and defaults to `http://localhost:8000`.
